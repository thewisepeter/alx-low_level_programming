#include "main.h"

/**
 * mul - multiplies two integers
 * @a: character to be multiplied
 * @b: character to be multiplied
 * Return: result
 */
int mul(int a, int b)
{
	int mul = (a * b);

	return (mul);
}
