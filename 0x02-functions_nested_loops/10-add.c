#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: number to be added
 * @b: number to be added
 * Return: Result
 */
int add(int a, int b)
{
	int add;

	add = (a + b);
	return (add);
}

